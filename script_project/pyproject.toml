[project]
name = "script-project-template"
version = "0.0.0"
description = "A template for python scripts."
readme = "coming"
requires-python = ">=3.12,<3.13"
dependencies = [
    "httpx2~=2.12.0",
    "python-dotenv~=1.2.0",
    "trio~=0.34.0",
]

[dependency-groups]
local = []
staging = []
prod = []
